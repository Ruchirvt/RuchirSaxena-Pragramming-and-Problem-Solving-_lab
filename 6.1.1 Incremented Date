# Type Content here...
day=int(input())
month=int(input())
y=int(input())
def leap(y):
	return (y%400==0)or(y%4==0 and y%100!=0)


valid=True
if y<=0:
	valid=False
if month<1 or month>12:
	valid=False
if month in [1,3,5,7,8,10,12]:
	max=31
elif month in [4,6,9,11]:
	max=30
elif month==2:
	if leap(y):
		max=29
	else:
		max=28
else:
	max=0

if day<1 or day>max:
	valid=False
	print("Invalid Date")
else:
	day+=1
	if day>max:
		day=1
		month+=1
		if month>12:
			month=1
			y+=1

	print(f"{day:02d}-{month:02d}-{y}")
